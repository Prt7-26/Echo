import Foundation

/// Package-wide metadata for the EchoSiri logic layer.
/// The Kit target holds everything that can be tested without a UI:
/// Codable protocol models, GatewayClient, EchoAPIClient, Stores.
public enum EchoSiriKit {
    public static let version = "0.0.1"
}
